apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homeassistant.fullname" . }}-test-connection"
  labels:
{{ include "homeassistant.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "homeassistant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
